<Page x:Class="P4Project2.Views.CharacterSelection_View"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:P4Project2.Views"
      xmlns:m="clr-namespace:P4Project2.Models"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      FontFamily="Perpetua Tiling MT"
      Title="CharacterSelection_View">
    <Page.DataContext>
        <m:Gladiator/>
    </Page.DataContext>
    <Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <DataGrid x:Name="DG_Gladiators" AutoGenerateColumns="False" ItemsSource="{Binding _Gladiators}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                    <DataGridTextColumn Header="Title" Binding="{Binding Title}"/>
                    <DataGridTextColumn Header="Class" Binding="{Binding PrimaryClass.Name}"/>
                    <DataGridTextColumn Header="Ascendancy" Binding="{Binding Ascendancy.AscendancyName}"/>
                    <DataGridTextColumn Header="Level" Binding="{Binding CurrentLevel.LevelNo}"/>
                    <DataGridTextColumn Header="Weapon" Binding="{Binding CurrentWeapon.Name}"/>
                    <DataGridTemplateColumn>
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Style="{StaticResource menu_Button}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <Button Click="Button_Click" Height="25" Width="100"/>
        </Grid>
    </Grid>
</Page>
